{
  "name": "swapperd-native",
  "homepage": ".",
  "version": "1.0.0",
  "description": "Swapper daemon for Atomic Swaps",
  "dependencies": {
    "menubar": "*",
    "ws": "^6.1.0"
  },
  "devDependencies": {
    "electron-packager": "^12.2.0",
    "electron-prebuilt": "^1.4.13"
  },
  "main": "main.js",
  "scripts": {
    "build": "electron-packager . Example --platform=linux --arch=x64 --version=0.36.0 --icon=Icon.icns",
    "start": "XDG_CURRENT_DESKTOP=Unity npx ./node_modules/electron-prebuilt/cli.js ."
  }
}
